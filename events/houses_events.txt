# HOUSES EVENTS
namespace = HOUSES

character_event = {
    id = HOUSES.1
    hide_window = yes   
    only_rulers = yes
    is_triggered_only = yes
    
    immediate = {
        if = {
            limit = {
                ROOT = {
                    mother = {
                        trait = sharingan
                    }
                    prisoner = no
                    NOT = { trait = incapable }
                    is_in_society = yes
                    society_rank < 3
                    society_can_rank_up = yes
                }
            }
        }
        ROOT = {
            add_trait = sharingan
        }
        if = {
            limit = {
                ROOT = {
                    father = {
                        trait = byakugan
                    }
                    prisoner = no
                    NOT = { trait = incapable }
                    is_in_society = yes
                    society_rank < 3
                    society_can_rank_up = yes
                }
            }
        }
        ROOT = {
            add_trait = byakugan
        }
    }
}
